{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-tetris-battle",
  "version": "1.0.17",
  "description": "Multiplayer Tetris Battle plugin for OpenCode TUI",
  "type": "module",
  "license": "MIT",
  "author": "Lars Hagen",
  "homepage": "https://github.com/lars-hagen/opencode-tetris-battle#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lars-hagen/opencode-tetris-battle.git"
  },
  "bugs": {
    "url": "https://github.com/lars-hagen/opencode-tetris-battle/issues"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "tui",
    "tetris",
    "multiplayer",
    "convex"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "prepack": "bun run typecheck",
    "release:patch": "bun run typecheck && npm version patch -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)",
    "release:minor": "bun run typecheck && npm version minor -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)",
    "release:major": "bun run typecheck && npm version major -m \"chore: release %s\" && git push && git push origin $(git describe --tags --abbrev=0)"
  },
  "files": [
    "README.md",
    "LICENSE",
    "index.ts",
    "tui.tsx",
    "tetris-battle.tsx",
    "tsconfig.json"
  ],
  "exports": {
    ".": {
      "import": "./tui.tsx"
    },
    "./tui": {
      "import": "./tui.tsx"
    },
    "./server": {
      "import": "./index.ts"
    }
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "@opentui/core": "*",
    "@opentui/solid": "*",
    "solid-js": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.28",
    "@opentui/core": "^0.1.105",
    "@opentui/solid": "^0.1.105",
    "solid-js": "^1.9.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "convex": "^1.36.1"
  }
}
