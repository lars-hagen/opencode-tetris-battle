{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-tetris-battle",
  "version": "1.0.4",
  "description": "Multiplayer Tetris Battle plugin for OpenCode TUI",
  "type": "module",
  "license": "MIT",
  "author": "Lars Hagen",
  "homepage": "https://github.com/lars-hagen/opencode-tetris-battle#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lars-hagen/opencode-tetris-battle.git"
  },
  "bugs": {
    "url": "https://github.com/lars-hagen/opencode-tetris-battle/issues"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "tui",
    "tetris",
    "multiplayer",
    "convex"
  ],
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "convex:dev": "convex dev",
    "convex:deploy": "convex deploy",
    "prepack": "bun run typecheck",
    "release:patch": "bun run typecheck && npm version patch && git push && git push --tags",
    "release:minor": "bun run typecheck && npm version minor && git push && git push --tags",
    "release:major": "bun run typecheck && npm version major && git push && git push --tags"
  },
  "files": [
    "README.md",
    "LICENSE",
    "tui.tsx",
    "tetris-battle.tsx",
    "tsconfig.json"
  ],
  "exports": {
    ".": {
      "import": "./tui.tsx"
    },
    "./tui": {
      "import": "./tui.tsx"
    }
  },
  "peerDependencies": {
    "@opencode-ai/plugin": "*",
    "@opentui/core": "*",
    "@opentui/solid": "*",
    "solid-js": "*"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.28",
    "@opentui/core": "^0.1.105",
    "@opentui/solid": "^0.1.105",
    "solid-js": "^1.9.12",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "convex": "^1.36.1"
  }
}
